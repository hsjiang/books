package com.books.application.common.network.retrofit

import android.content.Context

object ParamsBuilder {
    fun with(context: Context): HashMap<String, String> {
        val map = HashMap<String, String>()
        return map
    }
}